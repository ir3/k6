# 明細の「順」統合をDBに持たない設計(OrderSortedItems)

並び替え後表示・受注メモ・請求書Aの3箇所で使っている、`Orderpart`(部品番号あり)と
`NOrderpart`(部品番号無)を「順(SNo)」で一本化する仕組みの設計メモ。

## 背景・課題

- 旧ASP(`asp/jutyu111.asp`)は「見積」という一時テーブルに、部品番号あり/無を
  順であらかじめ一本化したものを持っていた。しかしこのテーブルは`MNo`列を持たず
  注文と紐付かないワークテーブルだったため、そのまま踏襲するのはやめた。
- 直前に`注文注釈`をそのままテーブル化(`order_annotations`)してインポートした後、
  「注釈は最大50文字程度でそんなに長くない、専用テーブルを持つほどではない」と
  判明し、結局`orders.memo`へ統合してテーブルを削除する、という手戻りが発生した
  (詳細は[TAX_RATE.md](TAX_RATE.md)と同時期の作業)。
- この経験から、「一本化した一覧が欲しいだけなら、まずDBに持たずその場で
  組み立てる」という方針に切り替えた。

## 結論: DBには何も持たない

`OrderSortedItems`(`app/services/order_sorted_items.rb`)は`ActiveRecord::Base`を
継承しないただのRubyクラス(PORO)で、対応するテーブルは存在しない。

```ruby
OrderSortedItems.for(order)
```

を呼ぶたびに、その場で

1. `Orderpart.where(mno: order.mno)`と`NOrderpart.where(mno: order.mno).where.not(tvalid: 0)`
   をDBから素直に読み直す
2. メモリ上で`sno`をキーにマージ・ソートする
3. `Item`構造体(`Struct.new(..., keyword_init: true)`)の配列を返す

これだけで、テーブルへの書き込みは一切行わない。

## なぜ蓄積しない・古いデータが残らないと言えるか

- Rubyのオブジェクトはローカル変数やインスタンス変数としてメモリ上に置かれるだけで、
  明示的にDB書き込み・ファイル書き込み・キャッシュ保存をしない限り、リクエスト
  (または帳票生成)の処理が終わればGC(ガベージコレクション)の対象になり消える。
  `OrderSortedItems`はそのどれも行っていない。
- `items`メソッドは呼ばれるたびに`Orderpart`/`NOrderpart`テーブルの「今の状態」を
  素直に読み直すだけなので、「古い結果がどこかに残っていて、それが徐々に古びて
  いく/溜まっていく」という状態自体が存在しない。前回の呼び出し結果を再利用する
  仕組み(キャッシュ)を一切持たないため、キャッシュ無効化漏れという種類の
  バグも起こり得ない。
- 呼び出し側(`OrdersController#sorted`、`SeikyuReport`、`JuchuMemoReport`)は
  いずれも、その処理の中で`OrderSortedItems.for(order)`を都度呼んでいるだけで、
  結果をどこかに保存して次回使い回す、という設計には一切なっていない。

## トレードオフ

- 呼ぶたびに`Orderpart`・`NOrderpart`・(明細ごとの)`Part`/`Kepart`をDBから
  読み直すため、クエリ回数は「結果をキャッシュする設計」より多い。
- ただし現状は注文1件あたりの明細数がせいぜい数百件程度であり、体感できる
  性能問題は出ていない。将来的に件数が増えてボトルネックになった場合は、
  `Rails.cache`等によるTTL付きの一時キャッシュを検討する余地はあるが、
  現時点ではその複雑さに見合うメリットがないため導入していない。
