# 消費税率の持ち方について

注文部品詳細画面(orders#show)に消費税額・消費税率変更機能を追加した際の検討記録。

## 背景・課題

- 消費税率は過去に何度か変わっている(5%→8%→10%、将来0%になる可能性もある)。
- 「取引管理No.(注文)ごとに、その取引に適用された税率を保持しているはず」と
  思われていたが、実際には**そのような項目はAccess DB(`db/access/fsdb.mdb`)の
  どのテーブルにも存在しなかった**(全30テーブル・全カラム名を"tax"/"zei"/"税"/
  "率"/"課税"/"%"で検索して確認済み)。
- 唯一近いものとして「取引条件」テーブル(`NO`=顧客コード、取引管理Noとは無関係)に
  `消費税`という列があったが、中身は`0`/`1`/空欄のみの**フラグ**(その顧客の取引に
  消費税を掛けるかどうか)であり、税率(%)ではなかった。
- `部品明細`テーブルには`TotalA`(税抜相当)と`Total2`という2つの金額列があり、
  「税抜合計と税込合計の差分から税率を逆算できないか」も検討したが、
  `Total2`は**全件0**で、実際には一度も入力されていなかった。
- 結論: 旧ASPコードのコメント(`旧ASPはSession値、デフォルト8%だったが現行税率に
  合わせる`)の通り、税率はDBに保存されず、**ASPのセッション変数として操作者が
  都度手入力していた**もので、そもそも過去の取引ごとの税率を復元する手段が
  データ上に残っていない。

## 過去分の扱い

日付(納期・出荷日など)から機械的に税率を逆算するルールも検討したが、
「税率切り替えのタイミングは個別事情があり、単純な日付境界では例外を救えない」
という判断から採用しなかった。

**方針: 過去分を自動的に正しい税率へ復元することはしない。** 必要になった時点
(過去の注文を再表示・再出力する必要が生じた時)に、その時点で分かっている
履歴的な税率を人が個別入力して上書きする、という運用にする。今回作った
「個別指定(取引ごとの上書き)」の仕組みが、そのまま例外救済の手段になる。

## 実装

### 個別指定 (`orders.tax_rate`)

- `orders.tax_rate` (nullable integer)。**nil = 個別指定なし、全体デフォルトに従う。**
- `Order#effective_tax_rate` — `tax_rate`があればそれを、無ければ
  `Order.default_tax_rate`を返す。画面・`XlsxReports::SeikyuReport`(請求書Excel)
  の両方でこれを使うため、画面表示と実際の請求書で税率がズレることはない。
- 画面(`app/views/orders/_order_part_tax_row.html.haml`)の入力欄を**空にして送信**
  すると`tax_rate`が`nil`になり、個別指定を解除して全体デフォルトに戻せる。

### 全体デフォルト (`ksystems`テーブル)

- 「画面から変更できるシステム全体の設定値」として、key-value型の
  `ksystems`テーブル(`Ksystem`モデル、`key`にユニークインデックス)を新設。
  税率専用のカラムを追加するのではなく汎用テーブルにしたのは、今後similar な
  全体設定が増えてもマイグレーション無しで対応できるようにするため。
- `Ksystem.get("default_tax_rate", default: ...)` / `Ksystem.set(key, value)`
- `Order.default_tax_rate` — `ksystems`から読む。行が無ければ
  `Order::FALLBACK_TAX_RATE`(10)にフォールバック。
- Menu画面(`app/views/menus/index.html.haml`最下部)に入力欄+「税率変更」ボタン。
  更新は`MenusController#update_tax_rate`(`PATCH /menu/tax_rate`)、
  管理者(`user_profile.state > 1`)のみ許可。

### なぜ「個別指定 > 全体デフォルト」の優先順位にしたか

全体デフォルトを書き換えても、既に個別指定済みの注文(=その時点で確定した税率)
は影響を受けない。逆に個別指定の無い注文は、常に「その時点の」全体デフォルトを
参照する。将来0%に切り替える際は、**切り替え前に「今10%で確定させたい注文」に
個別指定で10%を明示してから**全体デフォルトを0%にする、という運用が必要になる
(切り替え時に自動で一括バックフィルする仕組みは作っていない)。
